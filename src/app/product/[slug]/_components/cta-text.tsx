import { CtaText as ICtaText } from "@/types/product.types";

interface CtaTextProps {
  ctaText: ICtaText;
}

export default function CtaText({ ctaText }: CtaTextProps) {
  return <div>Enter</div>;
}
