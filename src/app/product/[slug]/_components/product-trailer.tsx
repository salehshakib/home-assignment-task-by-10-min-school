import { Medium } from "@/types/product.types";

interface ProductTrailerProps {
  media: Medium[];
}

export default function ProductTrailer({ media }: ProductTrailerProps) {
  return <div>Enter</div>;
}
